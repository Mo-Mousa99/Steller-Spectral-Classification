This directory stores raw SDSS FITS spectra.

The FITS files are not included in this repository because they are large.

To download the spectra used in this project, run:

Scripts/download_sdss_spectra.py

The script will download spectra directly from the SDSS data archive.